# Google OAuth client ID for the user-facing website.
# Google sign-in button is hidden when this is empty (recommended until the
# backend GOOGLE_CLIENT_ID is configured for this client).
VITE_GOOGLE_CLIENT_ID=
